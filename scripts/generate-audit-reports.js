const fs = require('node:fs'); const path = require('node:path'); const matter = require('gray-matter');
const products = fs.readdirSync('content/products').filter(x=>x.endsWith('.json')).map(file => JSON.parse(fs.readFileSync(path.join('content/products',file),'utf8')));
const posts = fs.readdirSync('content/blog').filter(x=>x.endsWith('.mdx')).map(file => { const d=matter(fs.readFileSync(path.join('content/blog',file),'utf8')).data; return { slug:file.replace(/\.mdx$/,''), title:String(d.title||''), description:String(d.excerpt||d.description||''), date:String(d.date||'') }; });
const categories = JSON.parse(fs.readFileSync('content/categories.json','utf8'));
const staticPaths=['','catalog','blog','contacts','podbor','cart','privacy','consent','about','delivery','payment','warranty'];
const urls=[...staticPaths.map(slug=>({type:'static',url:`/${slug}`.replace(/\/$/,'/') })),...categories.map(c=>({type:'category',url:`/catalog/${c.slug}`,title:c.title,h1:c.h1,description:c.description})),...products.map(p=>({type:'product',url:`/catalog/${p.slug}`,title:`${p.seoTitle||p.title} — ${p.article} — ЩУПЫ.РУ`,h1:p.title,description:`${p.title}. Артикул ${p.article}, производитель ${p.brand}. ${p.description||''}`.slice(0,300)})),...posts.map(p=>({type:'article',url:`/blog/${p.slug}`,title:p.title,h1:p.title,description:p.description,lastmod:p.date}))];
fs.writeFileSync('reports/urls.json',JSON.stringify({generatedAt:new Date().toISOString(),counts:{products:products.length,articles:posts.length,categories:categories.length,static:staticPaths.length,total:urls.length},urls},null,2)+'\n');
const duplicateGroups = key => { const m=new Map; urls.filter(x=>x[key]).forEach(x=>m.set(x[key],[...(m.get(x[key])||[]),x.url])); return [...m.values()].filter(v=>v.length>1); };
const titles=duplicateGroups('title'), h1s=duplicateGroups('h1'), descriptions=duplicateGroups('description');
const report=`# Базовый SEO-аудит\n\n**Сформирован:** ${new Date().toISOString()}\n\n## Инвентаризация\n\n- Framework: Next.js 16, App Router, React 19, TypeScript; страницы серверные и статически генерируемые.\n- Роутинг: \`src/app\`; товары — JSON в \`content/products\`, статьи — MDX в \`content/blog\`, категории и информационные тексты — JSON в \`content\`.\n- Обнаружено: **${products.length} товаров**, **${posts.length} статей**, **${categories.length} категорий**, **${staticPaths.length} статических URL**. В ТЗ заявлено 140 статей, но в репозитории фактически ${posts.length}; отсутствующие 15 нельзя включить без исходного контента.\n- Metadata формируется в page-модулях через Metadata API; canonical добавлен ключевым шаблонам. Open Graph есть глобально и на товарах/статьях.\n- JSON-LD: Organization глобально, Product и BreadcrumbList в карточке, Article и BreadcrumbList в статье.\n- Meta robots: глобально не ограничивает индексацию; robots.ts разрешает обход.\n\n## Дубликаты в источнике\n\n- Групп дублирующихся \`title\`: **${titles.length}** (страниц в группах: **${titles.flat().length}**).\n- Групп дублирующихся \`H1\`: **${h1s.length}** (страниц в группах: **${h1s.flat().length}**).\n- Групп дублирующихся descriptions: **${descriptions.length}** (страниц в группах: **${descriptions.flat().length}**).\n\n## Аналитика и цели\n\n- Яндекс.Метрика 109048844 подключена с webvisor, clickmap и trackLinks.\n- Google Analytics подключается только при наличии \`NEXT_PUBLIC_GA_ID\`; ID в репозитории отсутствует.\n- Специализированные события успешных отправок форм ещё требуют проверки в реальных кабинетах аналитики. Подтверждение Webmaster/Search Console невозможно выполнить из репозитория без доступа владельца.\n\n## Открытые ограничения\n\n- Юридические реквизиты и фотографии отсутствовали в исходных данных и явно обозначены TODO.\n- Реальную доставку SMTP нельзя подтвердить без production credentials; API возвращает диагностируемые статусы и не раскрывает внутреннюю ошибку.\n- Политика и согласие являются шаблонами и требуют согласования с юристом.\n`;
fs.writeFileSync('reports/audit-baseline.md',report);
